%h1 Listing recommendations

%table
  %tr
    %th Jobvert
    %th Re name
    %th Display index
    %th
    %th
    %th

  - @recommendations.each do |recommendation|
    %tr
      %td= recommendation.jobvert
      %td= recommendation.re_name
      %td= recommendation.display_index
      %td= link_to 'Show', recommendation
      %td= link_to 'Edit', edit_recommendation_path(recommendation)
      %td= link_to 'Destroy', recommendation, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Recommendation', new_recommendation_path
